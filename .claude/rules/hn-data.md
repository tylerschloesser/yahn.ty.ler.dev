---
paths:
  - "packages/hn/**"
---

# The HN data layer

Loaded when you touch `packages/hn/`.

**Read `docs/hn-api.md` first.** It is the complete, mined API reference — every endpoint,
every field per item type, the tombstone shapes, measured latencies and request counts. Do not
re-derive any of it from the live APIs, and do not write a field list from memory.

> **Status: stub.** Filled in during Epoch 1 as `packages/hn` is built. The comment-ordering
> spike's finding and the resulting `getCommentSource()` default go here.
