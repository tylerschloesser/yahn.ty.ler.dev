# Deploys a per-PR preview stack and comments the preview URL, status and
# timings on the PR.
#
# `concurrency.cancel-in-progress` is deliberately false: cancelling this job
# does not cancel the CloudFormation deploy it started, and the next push
# would then hit a stack stuck in UPDATE_IN_PROGRESS and fail. A new push just
# queues behind the run already in flight for this PR — and pr-teardown.yml
# shares this exact group name, so a close can never race a deploy for the
# same PR either.
#
# The comment reports deploy seconds and push-to-comment seconds so the
# preview's cost is visible on every PR without anyone timing it by hand. The
# old clone-per-PR preview measured ~6 min to create and ~4 min to destroy;
# these numbers are how the migration onto the shared preview distribution is
# judged. `date +%s` around the deploy step and from job start to the comment
# gives both without adding a dependency.
#
# The QR under the timings is a hosted image from api.qrserver.com: GitHub
# strips `data:` URIs and inline <svg> from comments, so a hosted image is the
# only form that renders as an image — and camo caches it, so an existing
# comment survives the service going away.

name: PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  id-token: write
  contents: read
  pull-requests: write

concurrency:
  group: pr-preview-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  preview:
    # Fork PRs get no preview, and that is correct rather than a limitation:
    # GitHub will not grant id-token: write to a workflow run triggered by a
    # fork PR, so the deploy could not authenticate anyway.
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    env:
      PR: ${{ github.event.pull_request.number }}
    steps:
      - name: Record job start
        run: echo "JOB_START=$(date +%s)" >> "$GITHUB_ENV"

      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      - uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1
          role-session-name: github-actions-pr-preview

      - name: deploy
        id: deploy
        run: |
          start=$(date +%s)
          pnpm --filter @yahn/cdk exec cdk deploy "Yahn-pr-$PR" --exclusively -c "pr=$PR" --require-approval never
          end=$(date +%s)
          echo "seconds=$((end - start))" >> "$GITHUB_OUTPUT"

      # KVS writes for a new preview hostname reach the edge on no published
      # SLA (AWS's own launch note says only "a few seconds"), so the stack
      # being CREATE_COMPLETE does not mean the router at the edge already
      # knows the hostname. Poll before handing the URL to Playwright.
      - name: Wait for the preview to answer
        run: |
          set -euo pipefail
          # This waits for the KeyValueStore write to reach the edge, which is
          # what makes a just-deployed preview hostname resolvable at all.
          #
          # It checks the status code explicitly rather than leaning on
          # `curl -f`, and that is the whole point: `curl -f` only fails on
          # >= 400, so behind the edge gate it treated the 302 as success and
          # this step silently stopped gating anything. Before the KVS key
          # propagates the router returns **404 "no such preview"**, so "not
          # 404" is the honest signal that the preview exists — which is all
          # this loop waits for. `/api/health` is in `auth.ungatedPaths` now
          # (cdk-core >= 0.2.2), so it answers **200** here; a 302 would mean
          # the exemption is gone. Either way the router resolved the host, so
          # the check stays "not 404" rather than pinning a code.
          url="https://pr-$PR.preview.yahn.ty.ler.dev/api/health"
          for i in $(seq 1 30); do
            code=$(curl -s -o /dev/null -w '%{http_code}' --max-time 10 "$url" || echo 000)
            if [ "$code" != "404" ] && [ "$code" != "000" ]; then
              echo "$url answered $code after $i attempt(s)"
              exit 0
            fi
            sleep 10
          done
          echo "$url never answered"
          exit 1

      # A bare `pnpm exec` resolves the binary directly: @playwright/test is
      # a root devDependency here, not the e2e package's own.
      - run: pnpm exec playwright install --with-deps chromium

      - name: e2e
        id: e2e
        run: pnpm e2e
        env:
          PLAYWRIGHT_BASE_URL: https://pr-${{ github.event.pull_request.number }}.preview.yahn.ty.ler.dev

      # Only when `e2e` itself failed, so a green run pays nothing and a run
      # where `deploy` failed — and `e2e` never wrote anything — does not upload
      # an empty artifact. `playwright.config.ts` already produces both paths on
      # CI: `test-results/` holds the traces (`trace: 'on-first-retry'`, which
      # fires because `retries` is 2 on CI) and `playwright-report/` the html
      # reporter's output. This step is only what keeps them past the runner.
      #
      # 7 days: a trace is megabytes, and it is only useful while the preview
      # stack that produced it still exists.
      - name: Upload Playwright traces
        if: always() && steps.e2e.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: playwright-pr-${{ github.event.pull_request.number }}
          path: |
            test-results/
            playwright-report/
          retention-days: 7

      # `always()`, so a failing preview still tells the PR where to look.
      - name: Comment preview URL
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          NOW=$(date +%s)
          TOTAL=$((NOW - JOB_START))
          DEPLOY_SECONDS="${{ steps.deploy.outputs.seconds }}"

          if [ "${{ steps.deploy.outcome }}" != "success" ]; then
            STATUS="❌ deploy ${{ steps.deploy.outcome }} — the preview may not exist"
          elif [ "${{ steps.e2e.outcome }}" = "success" ]; then
            STATUS="✅ deployed and e2e passed"
          else
            STATUS="⚠️ deployed, but e2e ${{ steps.e2e.outcome }}"
          fi

          gh pr comment "$PR" --edit-last --create-if-none --body "$(cat <<EOF
          ### PR Preview

          $STATUS

          - URL: https://pr-$PR.preview.yahn.ty.ler.dev
          - Commit: ${{ github.event.pull_request.head.sha }}
          - deploy ${DEPLOY_SECONDS} s · push → comment ${TOTAL} s

          ![Scan to open the preview](https://api.qrserver.com/v1/create-qr-code/?size=180x180&qzone=2&data=https%3A%2F%2Fpr-$PR.preview.yahn.ty.ler.dev)
          EOF
          )"
